#
# Sylpheed
# 

%site
http://sylpheed.good-day.net/sylpheed

%source
sylpheed-0.9.12.tar.bz2
local:wchar.patch;patch=1

%define builddir sylpheed-0.9.12

%build
export GDK_PIXBUF_CONFIG=/opt/Embedix/tools/arm-linux/bin/gdk-pixbuf-config

./configure %{config_opts} %{x_opts} %{gtk_opts} \
    --enable-gdk-pixbuf --disable-imlib
make

%install
make %{install_opts} install

%files
%{prefix}/bin
%{prefix}/share/locale/ja

%strip
!%{prefix}/bin/*
*.so*

%control
Package: sylpheed
Priority: optional
Section: mail
Maintainer: Takuya Murakami <tmurakam@mtd.biglobe.ne.jp>
Architecture: arm
Version: 0.9.12-1
Depends: glib, gtk, gdk-pixbuf
License: GPL
Cardinst: enable
Description: Sylpheed
 Sylpheed mailer
