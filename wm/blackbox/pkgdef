#
# blackbox
# 

%source
http://easynews.dl.sourceforge.net/sourceforge/blackboxwm/blackbox-0.65.0.tar.gz

%define builddir	blackbox-0.65.0

%build
./configure --prefix=/opt/QtPalmtop --host=arm-linux \
    --with-glib-prefix=%{cross_prefix} --with-gtk-prefix=%{cross_prefix} \
    -x-includes=%{x_prefix}/include -x-libraries=%{x_prefix}/lib
make

%install
make DESTDIR=%{destdir} install

%files
opt/QtPalmtop/bin/*

%control
Package: blackbox
Priority: optional
Section: X11
Maintainer: Takuya Murakami <tmurakam@mtd.biglobe.ne.jp>
Architecture: arm
Version: 0.65.0.1
Depends: xlibs
Description: Blackbox Window Manager
License: BSD

