HOW TO INSTALL XPKG

INSTALL FROM RPM PACKAGE

To create an RPM package, execute

 $ make rpm

or to build from tarball,

 $ rpmbuild --ta xpkg-*.tar.gz


BUILD FROM SOURCE WITHOUT RPM

 $ su
 $ make install

If you'd like to change prefix, execute

 $ make prefix=/usr/local install
