■■ xpkg : X/Qt プロジェクト パッケージビルドシステム

xpkg はパッケージをクロスコンパイルして、パッケージ(主にipkg)
を生成するためのツールである。

■ インストール

root になって、make install する。
RPM を生成したい場合は、make rpm で RPM パッケージを生成できる。


■ 利用方法

man xpkg を参照のこと。

■ ライセンス

COPYING を参照のこと。
